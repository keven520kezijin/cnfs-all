const getters = {
  device: state => state.device,
  loginUser: state => state.loginUser,
  partnerData: state => state.partnerData,
  marquee: state => state.marquee,
  newsData: state => state.newsData,
}
export default getters
